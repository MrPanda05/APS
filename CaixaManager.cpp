#include "CaixaManager.h"

void CaixaManager::fecharCaixa()
{
	//fecha o ultimo caixa do vetor _caixas do caixa dao
	// pseudo code is down here, almost done, just need to improve
	//_daoManager->getCaixaDao()->recuperarCaixa("1/23/4");
	//_daoManager->getCaixaDao()->atualizarCaixa();
}
